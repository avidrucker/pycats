# Learnings (TIL)

Per-session "Today I Learned" retrospectives. Each row links a dated entry; newest
at the bottom (chronological).

| Doc | Date | Agent | Themes |
|---|---|---|---|
| [TIL 2026-06-23 BANANA](./today-i-learned-2026-06-23-banana.md) | 2026-06-23 | BANANA | Reproduce-first viewable sims; physics-layer (not statechart) collision fixes; golden/parity as oracle; Project M fidelity (#5 solid sides, #1 jostle); sequencing same-file tickets; verifying subagent output. |
| [TIL 2026-06-23 CHERRY](./today-i-learned-2026-06-23-cherry.md) | 2026-06-23 | CHERRY | Re-derive git state live (session-start snapshot is stale); inspect a doc before deleting it; "committed" ≠ "merged+pushed"; committed plan indexes vs issues-as-source-of-truth. |
| [TIL 2026-06-23 DRAGONFRUIT](./today-i-learned-2026-06-23-dragonfruit.md) | 2026-06-23 | DRAGONFRUIT | A non-reproducing bug is a missing regression test (#7); surface contradictions before an outward-facing close; decrement-then-check cooldowns are off-by-one, seed N+1 (#10); prove a guard by reverting the fix; `pmtools close` exits 1 after success (read the banner); file-level (not just area-level) collision guard in orchestration. |
| [TIL 2026-06-24 CHERRY](./today-i-learned-2026-06-24-cherry.md) | 2026-06-24 | CHERRY | Merged ≠ closed (`Closes #N` in body, sweep git before trusting the open queue); the one-frame FSM-label lag — gate on the timer not the lagging state label (#8); parallel reset paths must clear the same fields (#9, follow-up #31); goldens move on behaviour fixes but emergent assertions don't — regenerate only after they pass; reproduce to ground truth, fix the state not the adjective (#9 "red"). |
| [TIL 2026-06-25 DRAGONFRUIT](./today-i-learned-2026-06-25-dragonfruit.md) | 2026-06-25 | DRAGONFRUIT | Verify-before-removing keeps killing the task's premise — "dead"/"unused"/"the bug is X" is a hypothesis, confirm before acting (D3 #70, #64, D4 #72; beware grep filters hiding a caller); when the obvious fix *overshoots*, suspect a second bug compensating for the first (#64 jab reach × `resolve_circle` facing); a revert-check that won't go red means the test is a Liar/tautology, not the code (#62 dodge); re-measure an audit's numbers before scoping to them (S4 #74: 1.18 MB → 139 KB via #64). |
| [TIL 2026-06-26 DRAGONFRUIT](./today-i-learned-2026-06-26-dragonfruit.md) | 2026-06-26 | DRAGONFRUIT | Verify capability claims empirically — pygame renders colour emoji on modern SDL_ttf (#103); flip the rendered *surface* not the *characters* for directional text-art, kaomoji and ASCII alike (#105/#110); a quantization "limitation" can be load-bearing — the int-position truncation is what makes replay/parity survive float noise (#80); park render-only state as a dynamic `getattr` attribute to stay out of another agent's lane (#108); an overlay tinted to its background disappears — contrast, don't match (#108); a research ticket's deliverable is a sourced ruling + only the follow-up it warrants, sometimes none (#80/#88/#117). |
| [TIL 2026-06-26 CHERRY](./today-i-learned-2026-06-26-cherry.md) | 2026-06-26 | CHERRY | The tracker moves under you in fleet work — re-fetch and re-locate by content (not stale line number) before every claim/edit; let the `pmtools claim` CLOSED-guard decide ownership (#86/#101). A reconciliation document isn't work and rots on the next refactor — move state into `@todo #N` puzzles + an `xfail` "→0" guard (#50/#104/#106/#112). A repro ticket's deliverable is one failing test; "doesn't reproduce" still ships a regression guard for the uncovered face (#101/#102). Fold into the owning issue — open a new ticket only for genuinely-new, unowned work; one `area:*` per issue (#91/#93). Fleet identity doesn't survive worktree teardown — stamp the fruit into the commit if you want post-hoc attribution. |
| [TIL 2026-06-26 ELDERBERRY](./today-i-learned-2026-06-26-elderberry.md) | 2026-06-26 | ELDERBERRY | PM combat numbers (frames/%, damage, weight, BKB/KBG, angle) drop in raw — only spatial values need one scale, `PX_PER_UNIT ≈ 5.4` anchored on Mario, which pycats' movement already matches (#119/#120). A new archetype key/state is golden-safe because of what the replay never presses — grep the actual timelines + `load_fighter_data` call sites, then regenerate goldens only once every diff is explained (#123/#124). A new fighter state is three coordinated edits — statechart leaf + legacy FSM state in identical order + `LABELS` — and isn't done until a scenario reaching it is byte-identical across backends (#124). Snap the sim, animate the render — keep easing in a render-only var that never enters the snapshot (#124). Don't ship a no-op seam you can't test — defer it to the slice where a non-default case makes it able-to-fail (#123→#126). |
| [TIL 2026-06-28 DRAGONFRUIT](./today-i-learned-2026-06-28-dragonfruit.md) | 2026-06-28 | DRAGONFRUIT | Answer the question actually asked, not the adjacent one — and grep the repo for the mechanic before researching externally (#141 clank was already in #133); re-read the literal question + confirm before closing a research ticket. Symmetric mechanics (shield decay, clank, hitstun) don't break a *perfect* mirror — a deterministic no-RNG sim needs an explicit resolver, which is what #61's match cap is (PM's match-timer analog); protect determinism, any future RNG is one seeded PRNG seam (#141/#144). A worktree is cut from main-at-claim-time — reconcile a suite failure against current origin/main before filing, it may already be fixed upstream (#137). pmtools close: keyword-guard wants a title keyword in the commit subject (`--skip-keyword-check` for corrected-scope titles); no-code research tickets close via `gh` + `pmtools release`. Extract a pure decision function from the IO/CLI shell for testability, `argparse default=None` to tell override from default (#61). One-off verification artifacts go in the scratchpad; the gitignored media dir is `repros/` not `repos/`. |
| [TIL 2026-06-29 DRAGONFRUIT](./today-i-learned-2026-06-29-dragonfruit.md) | 2026-06-29 | DRAGONFRUIT | A delegated/audit finding is a lead, not a fact — open the named `file:func` before filing; #189's two "follow-up bugs" (an "unwired" `StatechartScreenEngine`, a "mis-keyed" d-tilt) were both wrong on inspection (engine wired behind an env toggle; move resolves via a documented fallback), so I annotated #100/#142 and filed only the report-correction #194 instead of duplicate tickets. When an instruction's premise is false on verification, surface it and hand the decision back — don't comply or silently drop. Research-ticket premises go stale: #23's "air dodge zeroes Y" was already false on main; #148's "RNG-free controllers" was outdated by #166's seeded seam. Parallel per-subsystem auditors scale a codebase-wide sweep (~95 mechanics, 6 agents) but need a verify gate at synthesis. No-code research tickets close via `gh` + `pmtools release` (#191). |
| [TIL 2026-06-29 CHERRY](./today-i-learned-2026-06-29-cherry.md) | 2026-06-29 | CHERRY | A semantic golden regen red flag is a prompt to investigate, not a verdict — a vanished KO with 0% damage is a self-destruct, not weaker combat (#138). In fleet mode run the full suite the instant you claim; a worktree inherits whatever red another agent merged, so fix `main` first then `git merge origin/main` (#137). Shared combat/input code reached by test stubs must read optional Player/control attributes via `getattr`/`.get` (#137/#143). The repeatable doc loop (file → /issue-review → claim → write → close → tick) converges once review gaps are folded into the ticket template — every doc then scores READY 15/15 (#149–#175). In a multi-doc set, boundary discipline beats completeness: one concern per doc, cross-link siblings, fold in prior research rather than restating it (epic #147). |
| [TIL 2026-06-29 FIG](./today-i-learned-2026-06-29-fig.md) | 2026-06-29 | FIG | Before filing/implementing, scan all-state issues + docs — half my requests were already done or decided the other way (#17, #56/#69, #80, #16); surface the artifact, don't duplicate. A lesson only counts once it's in RULES, not the diary — codified close-discipline (#182) and the defensive-read rule (#179). No-code/decision/tracker tickets close via `gh issue close` + `pmtools release`, and a ruling isn't done until it's an ADR (#122/#174→ADR-0002, #29). When canon isn't look-up-able (PM air-dodge magnitudes), guess *loudly* — marked GUESS + tracker (#192) — and check whether a prior research ticket already gives the derivation (#120's `PX_PER_UNIT`≈5.4 → #195). |
| [TIL 2026-06-29 DRAGONFRUIT — value sourcing](./today-i-learned-2026-06-29-dragonfruit-2.md) | 2026-06-29 | DRAGONFRUIT | Engine-hardcoded constants aren't on rukaidata (no Self-Velocity in the subaction, no dodge attribute) — read the Melee decomp `doldecomp/melee` for the *model* and a faithful reimpl `meleelight` for the *literal* (`escapeair_force = 3.1`); rukaidata is only for scripted move/hitbox data (#215/#224). Corroborate before pinning a tuning value — three lines (reimpl literal + decomp model + wavedash back-solve) beat a remembered ≈3.1, then pin `DODGE_AIR_SPEED=17` (#222). "Ready to take" ≠ "completable here" — issue-review scored #215 READY but its datamine/playtest method isn't runnable headless; surface the ceiling, don't fabricate. An umbrella can't be closed to make progress — release the umbrella claim and land increments via closeable children (#192→#216/#215). Assert the model not the literal so a retune lands golden-clean (`vel == DODGE_AIR_SPEED`, not `== 14`) (#202/#222). Type the work before doing it — "organize the data" is a design choice (module vs JSON), so a `research`/architecture spike that spawns the refactor, not a lone refactor (#226). |
| [TIL 2026-06-30 FIG](./today-i-learned-2026-06-30-fig.md) | 2026-06-30 | FIG | Nearly every misstep this session was an unverified premise. Verify a ticket's *state* before calling it a blocker — I escalated a prose cross-ref ("overlaps ledge #14") into "blocked by #14" across ~6 turns; #14 was actually CLOSED (error #26 + memory pin). A "real battle" test must be *discriminating*: my #274 whiff-punish integration test passed even with the feature mutated off, so I rebuilt it to assert an off-cadence attack during the opponent's recovery (the #248 gotcha, one level deeper — revert-check the integration test, not just the units). Ground a READY ticket in the code before claiming — #277 scored 14/15 but the controller already approached+attacked and real footsies needs reach asymmetry the AI ignores (filed #285, parked #277); readiness ≠ value. Confirm a mechanic is in PM before speccing it — ledge-trump is Smash-4+, NOT PM (PM uses edge-hogging), so #267's trump slice was removed (#297). Root-cause in a real loop before accepting the reported cause — the fireball "spawn-height bug" was cross-elevation flat-travel; Nalio shipped Luigi's flat fireball, fixed as gravity+bounce (#263/#266). An umbrella isn't a work unit — slice children one at a time, WIP=1, record the decision on the ticket (#250 council). |
| [TIL 2026-06-30 GRAPE](./today-i-learned-2026-06-30-grape.md) | 2026-06-30 | GRAPE | Run `/issue-review-skill` right before claiming — batch-filed tickets go stale (F3 #321's approach was invalidated by S6 #304), and a bundled ticket (H-b #317, three entities) scores NEEDS WORK; re-scope to a single deliverable first. Pin behaviour-neutrality to byte-identical goldens AND the render-parity test — they cover different surfaces (a "render-only" change flipped the parity test #239/#330). Trace per-frame ordering before a "move the decrement" refactor — set-and-ticked-same-frame state can't move to a top-of-frame tick without an off-by-one (S1 #273 hitlag, S4 #289 getup). A guard written to a rule red-catches the first live violation — #339's ADR-0004 boundary immediately flagged physics.py's `pg.sprite` hint (fixed via a structural Protocol). Record design rulings as ADRs before coding (architect-then-courier; #320→ADR-0004, #318); a flag kept in sync with a computable value is a shadow — delete it and derive (`done_attacking` #321; the `owner` back-ref #304). |
| [TIL 2026-07-01 DRAGONFRUIT](./today-i-learned-2026-07-01-dragonfruit.md) | 2026-07-01 | DRAGONFRUIT | A stale umbrella tracker mis-routes you to already-done work — I nearly duplicated whiff-punish (#274 shipped) 3× against #250; verify each child's live state + grep the code, then sync the body (extends error #26). Golden-safe by *default-identity*, not by hand: gate a feature off-by-default so its golden-path default is an exact identity, then assert byte-identity/parity (#327 no-smash cat, #345 `standard`⇒`round(base*1.0)`, #312 level-less flags off → #418). An AI behavioural test must both drive the real `run_battle` loop AND go red with the feature off — hit twice: a melee-poke test the normal attack also satisfied (fixed by a below-the-lip foe, `dy>60`), and a recovery `y`-comparison broken by KO→respawn to `y=-1000` (switched to "emit-in-loop") — revert-check the integration test (#404/#409/#413, #248/#370→#418). Never `os.environ.setdefault` at a test-module top level — it poisoned the whole session (15 save/load tests) (#345, memory pin). When a spike defers a slice's design, decide it *in the child ticket* (input scheme #383, edge-hog-vs-edge-guard precedence #413) — the review's real job was catching the undecided fork on my own ticket. |
| [TIL 2026-07-01 ELDERBERRY](./today-i-learned-2026-07-01-elderberry.md) | 2026-07-01 | ELDERBERRY | A held key is one press, not a stream — edge-triggered inputs (jump/dodge) must be PULSED, so a situation-aware policy that keeps asserting `up` still jumps once then locks (#367/#369). Narrow a sim-behaviour fix so goldens + seeded battles stay byte-identical, and pick the separating threshold from measured data (normal runs ≤40f vs the 643f stall → gate at 90) (#369/#368/#379). A legitimate fix can invalidate a seeded test's *premise* — decide "perturbed the seed" vs "asserted the bug" and rewrite the assertion; never re-seed to green (#379/#309/#352). Controller code reading `a.fighter.*` must `getattr(default)` — stubs are minimal namespaces (hit 3×: #291/#368/#379). `cat`/`grep` doesn't satisfy the Edit read-gate in a worktree — Read the exact worktree path first (six EDIT_PRECOND rows until I made it a habit). Validate a fix's *design* against domain authenticity before building — a blanket idle-timer is a band-aid AND unfaithful (kills legit spacing), so #376 reshaped #368 into a narrow no-progress backstop. |
| [TIL 2026-07-01 CHERRY](./today-i-learned-2026-07-01-cherry.md) | 2026-07-01 | CHERRY | The Options "freeze" was a screen that never repainted — `game.py`'s unguarded module-level loop had no `options` render branch, invisible to headless tests that call `ScreenStateManager.render()` directly; extract the dispatch to a testable `screen_render` fn (#386). A memo cache that omits an input serves stale output — `_compose_mixed` keyed by authored (not scaled) size froze mixed menu text across font-scale changes while uncached title text resized (#401). When content can't fit (9 rows at 2× on 960×540), that's the user's call — surface it (AskUserQuestion → scrolling), then build on a pure, display-free `menu_layout` module (adaptive columns + scroll-to-selected, #402). Don't force the single-focused-row button widget onto char_select's 2D dual-cursor grid — ship the two menus that fit, split the misfit to a decision ticket (#360→#416). A 15/15 ticket carries decisions, not "resolve in design" — the Council of Yegors converged on freezing every open question into the body (#414). Derive test expectations from live structure (`len(rows)`), never a hardcoded count — a concurrent merge adding the #345 font_scale row (8→9) broke hardcoded grid-nav sequences twice; merge `origin/main` early and often. |
| [TIL 2026-07-01 GRAPE](./today-i-learned-2026-07-01-grape.md) | 2026-07-01 | GRAPE | `pmtools close` REBASES, so a long slice's merge-commits + regenerated goldens re-conflict every attempt — collapse to one clean commit on the current `origin/main` tip (`reset --soft` + regen fresh) then close (fleet-race, level 2; #311). Confirm a "this re-invents X" claim by grepping before reverting merged/tested code — #405's watch overlay really did duplicate #21's `InputHistory`, so the revert was right, landed via its own closeable ticket (#435) with the correct approach as a separate follow-up (#434). A DEV ticket that defers its own design to "the spike" isn't build-ready — resolve/lock the forks + add machine-verifiable acceptance in the ticket first (#405 rewrite, #413's LOCKED block). The orchestration snapshot decays fast — re-validate the `claims` array right before claiming (#413 was taken minutes after I assigned it). Hand feel-changing forks to the human via AskUserQuestion, don't silently bake gameplay (#311 evict + windowed getup). A surprising *semantic* golden-summary shift is a lead — trace it to ground truth before regen; #311's full_match 40→190% exposed a real pin-absorbs-knockback gap (#400). A "no orphans" completeness guard must compare against an independently-authored set, never one derived from the thing under test, or it can never fail (#233). |
| [TIL 2026-07-02 GRAPE](./today-i-learned-2026-07-02-grape.md) | 2026-07-02 | GRAPE | A user's bug report is a lead, not a spec — grep the handler first: the #453 "Esc doesn't back out of sub-menus" report was wrong on mechanism (Esc was hold-to-quit + fullscreen-exit, never a back key; back was on B), and filing it verbatim would have baked a false "used to work" premise (RULES.md "verify a finding" applies to user reports too). `grill-me` before an FSM change pays for itself — the hold-Esc ambiguities lived at the awkward nodes (stats-on-early-quit vs `playing→char_select`; the "no instant Esc" vs tap-exits-fullscreen collision), so lock the full source→destination table first and each row becomes one red→green test. A failing test that mutated a global by hand poisoned ~19 untouched files — `test_toggle_off_prevents_quit` stubbed `settings.load` and threw before its last-line restore, leaking the stub session-wide (KeyError cascade); use the `monkeypatch` fixture/`try-finally`, and read collateral failures in untouched files as a leaked global (cousin of the #345 `os.environ` pin). Deleting a test is fine when coverage moves to a better public-interface test — swapped the internals-poking `test_hold_esc_integration.py` for `test_hold_esc_navigation.py` (drives real `update()`/`get_state()`, survives the tick-relocation refactor), justified in the commit + revert-checked. |
| [TIL 2026-07-02 CHERRY](./today-i-learned-2026-07-02-cherry.md) | 2026-07-02 | CHERRY | Reproduce the failing observable before writing the fix — the #424 edge-hog self-KO was masked by the level-9 `recover` branch until I exhausted `jumps_remaining`, and a blind fix would have shipped a test green with OR without it. `git checkout <file>` to undo a revert-check wipes an *uncommitted* fix (drops to HEAD = the claim commit) — snapshot or re-apply + grep before committing (standing memory, bit me again). Don't manufacture a red for a facet you can't reproduce — #424 facet 2 (walk-off self-KO) was already safe (`EDGE_HOG_RANGE` + edge collision arrest the walk), so I shipped a labeled characterization guard + a written finding, not a fake failing test or a speculative fix. Extend a shared render primitive behind a default-identity kwarg (`draw_menu_button(pressed=False)`, #332) so goldens stay byte-identical by construction. Split a build coupled to a gated decision into its own `decision:` ticket (#332 audio → #445; #424 ledge-PX → research #458). |
| [TIL 2026-07-03 ELDERBERRY](./today-i-learned-2026-07-03-elderberry.md) | 2026-07-03 | ELDERBERRY | Review your own ticket to READY before building — resolving the deferred decisions on paper (issue-review 10/11→15 for #440/#471) removes mid-TDD redesigns, and the review fold-in (apply a saved set by *wholesale replace*, not sequential rebinds) is what makes #440's key-swap test pass. A scoping spike's best output is a shared foundation: #464/#465 both hinged on the missing text-entry, so I filed one reusable widget (#471) that two features consume instead of two designs. Split the pure model from the pygame surface and land the core first (#439→#447→#455; #440 store vs #463 UI; #471 widget vs hosting) — screens get only `frame_input.pressed`, so the capture flow lives in a pure controller driven by synthetic pressed-sets. A new capability with a *byte-identical default* keeps goldens/parity green — a `dict`-subclass `Keymap` drops into `controls[...]` unchanged; nickname `None` → identical "P1"/"P2". Verify the domain before the ticket's premise — the cats are jab-only (no launcher → a real KO is impossible, only self-destruct, #395/#398); `char_name` is win-attribution identity so the nickname is a *separate* field (#465); and "specials can't be scripted" was wrong (a user pushback caught it — `special` was already in the demo keymap). Assert a feature in its meaningful *window*, not "somewhere" — the showcase test stayed green while 5/7 beats misrepresented their captions (#395→#397), and inclusive caption windows double-render when `end == start` (#419). Run the *full* suite — wedging `_update_keybind` into `update()`'s body orphaned the nav code and reddened 15 existing options tests the keybind tests couldn't see. |
| [TIL 2026-07-03 FIG](./today-i-learned-2026-07-03-fig.md) | 2026-07-03 | FIG | Calibrate before executing a planned sweep — the #410 magic-number epic's "formula-dense first" ordering was stale (combat/systems already factored via ADR-0003; the debt was render/UI), so 10min of recon reordered a 7-child epic; report "already clean" as a finding. An identity refactor needs a proof, and the proof needs its own able-to-fail check — where goldens are thin (`screen_parity` is an FSM state-trace, not pixels) hash `render()` across states before/after, then flip one constant to confirm the harness actually reds (#420/#433/#450). The stale-`.pyc` footgun: two same-second `sed` flip/restore edits with identical file size leave mtime+size unchanged, so CPython serves cached bytecode — clear `__pycache__` / use `python -B` around revert-checks. Convention-once over marker/constant soup — marking ~27 near-identical position-approximation comments with `⚠` is the exact #448 "marker soup"; mark the convention once (docstring note), and in the config grouping (#450) merge genuinely-duplicated values but *refuse* coincidentally-equal font sizes (different roles → don't couple unrelated screens). Research produces a design *choice*, not a unilateral fix — the walk-off jump ruling (#466) was unambiguous PM but collided with open game-feel (#458), so I surfaced A/B to the reporter; chasing #473's edge cases then found the real divergence (airborne spawn, no invincibility) → spec #480 → full-PM epic #482. |
| [TIL 2026-07-04 ELDERBERRY](./today-i-learned-2026-07-04-elderberry.md) | 2026-07-04 | ELDERBERRY | A function that runs only inside a memo/cache needs its new input in the cache *key*, not just on the object — `draw_player_name`'s nickname (#478) is drawn through the cached `_cat_body_surface` composite, so nickname had to enter the key or a change serves a stale surface (proved with a cache-bust test; echoes CHERRY #401). Host new nav state in the adapter, not the collaborator whose tests lock it — the keybind "Schemes..." row (#463) lives in the `OptionsMenu` adapter so `KeybindMenu`'s action-only nav (and its 7 tests) stay untouched. A cohesive state machine can land ahead of some of its tests — recover the able-to-fail proof with a scoped mutation sweep: neutralising the four `keybind_store` calls reddened exactly the 5 persistence tests while the 3 pure-logic tests held (#463). The `area:*` lane is necessary but not sufficient — a grep showed #478/#469/#336 all edit `render_battle.py` (`draw_player_name`/`draw_controls`/`draw_hud`), so hold one, never tell agents to coordinate (5a-bis). The orchestration snapshot decays — #497 was assigned to me but another agent had already closed it (`a7cae73`); verify-and-stop, don't redo. An explicit owner directive overrides the tool-owned-push rule (direct-to-`main` tests overview) — but `git pull --rebase` first so the manual commit still respects the fleet. |
| [TIL 2026-07-04 DRAGONFRUIT](./today-i-learned-2026-07-04-dragonfruit.md) | 2026-07-04 | DRAGONFRUIT | A ticket's premise is a hypothesis — I ran the real `run_battle` loop and watched Nalio's fireball already fire on untouched `main`, so #429's "specials can't be scripted" was false (closed as already-satisfied, unblocked #432); verify the premise before building, surface a false one as a finding not silent work. Canonical data corrects your own ticket — rukaidata showed Kirby's d-smash is a *simultaneous* front+back splits-kick, not Marth's temporal split (#459), and refuted the "blocked by engine prereqs" framing (the charge engine is character-agnostic; fast-fall is air-only, smashes are grounded data). The sim is the oracle for frame-tuned choreography — dump per-frame positions to find the grounded/facing-right insertion point, then prototype the insert+shift and prove the downstream roll/ledge beats don't desync *before* editing (#432). Ground research in grep'd repo state, and let a deeper follow-up correct the parent audit's quick read — #497 downgraded my own #470 "Inspector" finding to a justified spy and found the project already fake-friendly (seeded-RNG DI, frame-based time, `tmp_path` persistence); `ruff` dominates `pyflakes`, so #486 was superseded by #492 (#487). Author ≠ reviewer — my `/issue-review` 15/15 on my own #462/#504 is advisory; route your own filed tickets to another agent and lean on the green gate, not your blessing (#459). A lesson expires unless it's in RULES.md — #418 codified golden-safe-by-default + discriminating-AI-test; and set `PYTHONPATH` to the *worktree* for probes of worktree-local edits (a `StopIteration` footgun). An empty bug queue is a valid clean state — don't fabricate out-of-scope work; point at bug-*finding* instead (#504). |
| [TIL 2026-07-05 DRAGONFRUIT](./today-i-learned-2026-07-05-dragonfruit.md) | 2026-07-05 | DRAGONFRUIT | A child process can't chdir its parent — so fix the *shape*: make `pmtools close` runnable from the main checkout (pmtools#104) so the shell is never stranded in the deleted worktree, instead of bolting on a post-teardown "return to main" (pmtools#105, closed as unneeded once you always close from main). A deferred follow-up can *evaporate* once the primary fix reframes the problem — re-derive its need, don't build on the inertia of having named it. Never document a not-yet-true form — verify the dependency actually landed (CLOSED state **+ merged code**, not "the user said so") before flipping RULES/CLAUDE to `cd <main> && pmtools close <N>` (#519 workaround → #526 clean form). Dogfood a workflow fix on its *own* close and record the **observed** result — #526/#528 closed from main gave exit 0, shell stays in main, no strand (I'd read the code first, the dogfood confirmed). The pre-close error self-audit is a silent-failure backstop: I skipped it every close, so two `NETWORK_FAIL`s (#528) + a `GIT_FAIL` rebase conflict (#511) + a probe `StopIteration` (#432) went unlogged until a human asked — backfilled rows 46-50, filed #533 to mandate an `error self-audit: …` line in every closing comment (an apostrophe in "session's" even broke the single-quoted `pmtools error log` arg). Research corrects the ticket's own framing — "should Nalio & Birky fall the same?" isn't about terminal fall speed (~1.28 vs ~1.2, close) but **gravity + fast-fall**, and weight is knockback not fall; ground a value question in the attribute model (#528). When a follow-up's *discipline* (DEV vs ARCHITECT) is genuinely unclear, that ambiguity is its own ticket, not a guessed label (#530). |
| [TIL 2026-07-05 CHERRY](./today-i-learned-2026-07-05-cherry.md) | 2026-07-05 | CHERRY | When a task needs many subjective numbers, find a rubric that *derives* most of them — ICE's Confidence fell out of the `issue-review` verdict and Ease out of scope/specificity, leaving only Impact for the human to review via the diff (#449). A tiebreak baked into a rounded score column silently vanishes — `1/(issue×1000)` rounds away at 5 decimals, so store the clean `I×C×E` for display and break ties explicitly in the sort key `(-score, issue#)` (#449). An "unsafe fix" label means "the tool can't prove it," not "it's unsafe" — once F401 confirmed the UP006-orphaned `typing.List` imports were annotation-only (the lone textual "List" was a docstring word), the *safe* F401 fix removed exactly the dead names, beating UP035 `--unsafe-fixes` (#524). Dedup a requested *set* against the tracker before filing any of it — a search found epic #476 already owned 2 of 3 asks + closed #156 did the GameCube feasibility, so I fit the catalogue in as thread 2 and used `Sequenced after: #477` to capture-without-working rather than mint duplicates (#553/#554/#555). A protocol that gains a parameter needs its test doubles conformed — threading `inputs=fi` into `presenter.show` reddened 3 stale fake presenters + a `__new__`-built partial; a stale fake failing is the change working, so grep `def show(` across `tests/` too (#434). Re-read the worktree's live CLAUDE.md over remembered tooling behavior — pmtools#104 means close from *main* (exit 0) not the worktree (exit 1, strands the shell). |
| [TIL 2026-07-05 GRAPE](./today-i-learned-2026-07-05-grape.md) | 2026-07-05 | GRAPE | Don't rule "refuted" on external mechanics from a chain of inference — pull a verbatim primary quote (`brawllib_rs`/PMDT/OpenSA) and label the rest inference; #520's "PM composes nothing" was really a two-layer model (Layer 1 single mutually-exclusive body-state, Layer 2 composing respawn/Star overlay), corrected in #537, and my interim Metal/Loupe "invuln flags" were misreads (knockback-resistance / off-screen chip-damage) — proposed as a rule in #562. Concurrent `gh issue create` mints numbers in completion order, so #535/#536 landed swapped into a committed doc + two comments; two root causes (stated-before-verifying + filed-concurrently) → ratified RULES clauses A+B (#541): verify a number by a `gh` lookup before stating it, and mint IDs/refs (`gh issue create`/`pmtools claim`) one at a time, never concurrently. Backticks in a double-quoted `gh --body` execute as shell — write the body to a file and use `--body-file`. A self-proposed governance rule still runs propose→ratify→apply (guide-human-decision on #541); never edit RULES.md without explicit human ratification (#472 precedent). |
| [TIL 2026-07-05 CHERRY (2)](./today-i-learned-2026-07-05-cherry-2.md) | 2026-07-05 | CHERRY | When work-tracking state lives in labels, edit the label not the generated artifact — `docs/roadmap.md` regenerates from `v1`/`post-v1`, so "add to the roadmap" = apply the label; #567 appeared in the deferred column automatically. Probe the sim's state timeline before scripting a demo beat — a 15-line dump of P1's state/x across frames gave the grab (f501), hang auto-release (~f621) and getup states up front, so the #421 ledge-recovery inputs landed right the first time (then revert-check the new assertion). Run issue-review on your *own* filing and act on it like a reviewer — it asked "does `statecharts@v0.0.1` actually contain the symbols pycats imports?" (→ load-bearing AC3, #583) and caught that a flat "no line refs" rule over-reaches against commit-pinned permalinks + quoted tool output (#579). Validate an install doc against the code's real import graph, not the old README — the prior quickstart installed only `pygame-ce` but the live game imports `statecharts` (`fighter_chart`), so a clean clone `ModuleNotFound`'d (#577, err 64). A pinned-dependency manifest is only proven in a *fresh* venv rebuilt from the pin, not the editable dev env — that clean run also exposed `imageio` as an undeclared already-in-use dep, 2 tests red until declared (#583, err 66). Reference locations by named landmark (function/class + file, or section heading), not a raw line number that rots — filed and immediately practised (#579, now RULES → "Referencing code & docs"). |
| [TIL 2026-07-05 ELDERBERRY](./today-i-learned-2026-07-05-elderberry.md) | 2026-07-05 | ELDERBERRY | An internal outline seam is a z-order bug — the body↔tail ring line (#585) was each part outlining itself and drawing halo+body per-part; split the composite into `(ring_layer, body_layer)` and draw ring→tail→body so every outline sits behind every body pixel, while keeping the merged `_cat_body_surface` byte-identical so the parity oracles need no changes. A value with no citable source is a decision, not a guess — crouch *height* isn't in rukaidata's scripted data, so #565 surfaced ratio-matched vs Kirby-low options to the owner via `AskUserQuestion`, recorded the answer TUNED, and implemented in a *separate* DEV slice (#589, which also had to re-author the crouch hurtbox circles combat tests). Cite primary not inference in the ticket *setup* too — I filed #592 asserting crawl exists in Melee; it's a Brawl mechanic PM inherits (err 75, the #520→#537 lesson). A generated doc's source of truth is its inputs — `docs/roadmap.md` regenerates from `v1`/`post-v1` labels, so "file crawl to the roadmap" = the `post-v1` label on #603, not a hand-edit of the stale snapshot. A colour assertion must exclude same-coloured siblings — the #585 seam test counts tail-body pixels shown in the ring colour (not the same-coloured name label), proven able-to-fail 41→0 (the #572 pitfall). `pmtools release` is positional-only (`release <N>`, no `--as`, err 71). |
| [TIL 2026-07-05 GRAPE (2)](./today-i-learned-2026-07-05-grape-2.md) | 2026-07-05 | GRAPE | A direct human instruction outranks a project rule — told "commit and push," I did neither, inventing an unrequested PR (#593) and citing a rule to justify it (err 74); when an instruction collides with a rule the only moves are do-it or ask, never a fabricated third path, so filed #600 to carve out that explicit in-session authorization permits a direct `git merge` + `git push origin main`. Don't cite a workflow that isn't in the repo — I asserted "we use PRs" but `grep` finds none and `git log --first-parent` is dead-linear (pmtools pushes straight to main), so verify a process against the repo before invoking it (#601 scrubs the stray "PR" references). Separation of concerns is structural, not taste — a `yegor-personas` council moved area-label enforcement out of the game repo (generic rule→pmtools, per-repo taxonomy→config/registry, prose→local), closing #570 `wont-do`/`superseded` and consolidating in pmtools#109. Ship the interim fix when it's correct/cheap/reversible but label it interim and point at the durable owner — #576's one-line RULES `area:docs` sync landed now, canonical-source decision deferred to pmtools#109. |
| [TIL 2026-07-05 BANANA](./today-i-learned-2026-07-05-banana.md) | 2026-07-05 | BANANA | Verify a delegated catalogue's *existence* claims, not just sampled values — a phantom `LEDGE_HANG_FRAMES` (removed #475) rode a subagent list into the shipped #580 audit + #582 ticket, caught only at implementation (err 72). Give the opinion; the human-gate is on the outward *commit*, not on recommending — I over-deferred on the #584 decision ("you decide") when the human wanted a source-backed pick, the mirror of over-acting (err 61 filed a README ticket but did the whole assessment): opine freely, act on commits only on go-ahead. Fold review feedback as a written architect amendment *before* couriering and elevate proving-test notes to required — #598 locked the design in a comment, then the able-to-fail exercised the novel `PLAYER_SIZE`-tuple path, not a scalar. Grep every `.value` consumer before widening a shared type (`Provenance.value`→tuple) and revert-check the widened path. `FOUND` means "sourced," not "verified-correct" — #581's smash-charge 60/1.4 were Brawl values, superseded by PM's 59/1.3671 (#599). |
| [TIL 2026-07-06 GRAPE](./today-i-learned-2026-07-06-grape.md) | 2026-07-06 | GRAPE | Verify a tool against its own source before writing its run recipe — I grounded the brawllib_rs datamine recipe (#614) in the clone's `readme.md`, `examples/*.rs` (the `-d`/`-m`/`-f`/`-l` flags, the `first_active_frames.rs` subaction→move map) and a `grep` of `src/high_level_fighter.rs` (serde `Serialize` present, `serde_json` absent), so no flag/name/JSON-capability was transcribed from how Rust CLIs usually look. Name the control-flow asymmetry before designing a shared seam — #507's two surfaces are inverted (CLI `run_battle` blocks its pull-loop to dwell, golden-safe *because* the sim can't advance while blocked; the in-game FSM owns cadence and can't block), which makes "embed `run_battle` in the tutorial screen" a trap and forces the seam — a pure non-blocking per-frame reducer + a steppable runner (deferred surface filed as epic #619). A ratification gate means file + present + hold the close, not auto-close — #514/#515 were blocked until a human confirms #507's design is thorough, so I committed the doc but held `pmtools close`, presented via `AskUserQuestion`, and closed only after sign-off. Gates nest, and provisioning belongs in the machine's repo — #614's run is gated (Rust toolchain + copyrighted `.pac` files) and even the JSON path had a nested gate (`serde_json` isn't a dep); the "update my machine's tooling" follow-up went to the dotfiles repo (`linux-mint-setup#2`, its own labels, no pmtools), not the game repo. |
| [TIL 2026-07-06 CHERRY](./today-i-learned-2026-07-06-cherry.md) | 2026-07-06 | CHERRY | A verbatim primary quote isn't confirmation when it's *ambiguous about an observable* — I "confirmed" that shield-break dizzy cancels on becoming airborne from SmashWiki's "any airborne state *induced* upon a stunned character will cancel the stunned state immediately," but "induced" means hit/knockback and a fighter is dizzy *and* airborne during the shield-break pop-up; the user caught it with one question, so for a frame-to-frame behaviour claim confirm with a replay/frame check, not a face-value sentence (sharpens the cite-primary rule; err #81, correction on #610). Issue-review your *own* filings — running the rubric on my own #613 caught that its AC "dizzy goes airborne → fall" was wrong (a hit should land in hitstun), a self-filed ticket earns no trust discount. When a fix's premise is unverified, reframe that ticket `bug`→`research` before coding and check which siblings are independent — #613 became research while #612 (drop-through gate, independent quotes) still shipped. A declarative registry makes a parity feature a one-record add — #597's `RECHARGE` bar was one `StatusSource`, #612's fix one clause in `should_prevent_drop_through`, so the budget goes to the able-to-fail test. Scope the formatter to changed files — `ruff format <diff files>`, never `ruff format pycats/` (carried err #76 forward, applied clean in #612). |
| [TIL 2026-07-06 BANANA](./today-i-learned-2026-07-06-banana.md) | 2026-07-06 | BANANA | A tuning-value correction can delete a golden's reason to exist — #599's PM smash nerf (1.4→1.3671, 60→59f) dropped the combat golden's marginal KO (`'ko' never reached`), so I restored the scenario (34→36 jabs) to *reproduce* the KO and did a semantic regen, rather than regen the KO away or weaken the `assert "ko"`. Scope the formatter to changed files — `ruff format pycats/ tests/` reformatted 193 files (err #82), recovered by `git checkout`-ing 186 back to HEAD (same class as CHERRY's #76 today). Verify a doc is *actually* stale before "fixing" it — the #595 reference doc was already accurate (a flagged "downgrade to contested" had been retracted), so only a post-change refresh was warranted; editing on the stale flag would have introduced an error. A human ruling records a decision, not authorization to implement the follow-up — I announced "proceeding to the RULES edit" after #618 was ratified, without a go-ahead (err #80, mirror of over-deferring). Fleet clones may have no pre-commit hook (`git rev-parse --git-path hooks/pre-commit` absent), so unformatted files leak onto main — the on-demand `ruff` run is the only gate; verify your own files and treat pre-existing main drift as a separate ticket. |
| [TIL 2026-07-06 DRAGONFRUIT](./today-i-learned-2026-07-06-dragonfruit.md) | 2026-07-06 | DRAGONFRUIT | A grep hit is a candidate, not a verdict — #601 asked to scrub stray "PR" refs, but the sweep mixed live workflow claims (fix), a TIL/README whose *subject* is the invented-PR error (keep — the record names the word), and a `pr` code-variable homograph (skip); edit the hits matching *intent*, not every hit matching *pattern*. Put the emoji-using generator at repo root, not in the package — #607's `grep -rn '🟢🟡🔴' pycats/` acceptance forbids circles in source, but the generator's status→circle map needs them, so `parity_report.py` lives at repo root (like `bench.py`) and the invariant is enforced by directory boundary, not by banning the glyph. On a generated-and-committed artifact the meaningful TDD reds are two: *file-absent* (the genuine first red before generating) then *stale* (able-to-fail via a `FOUND`→`TUNED` monkeypatch) — and the generator must read `provenance.TUNING_PROVENANCE` through the module so the monkeypatch reaches it. A machine-global file created outside the repo isn't in the commit diff — #568's `~/.claude/CLAUDE.md` rule text is untracked, so I named it in the close comment or the one-line RULES pointer would look like the whole change. Verify-then-close an umbrella — #451 read "closes when #607 lands," but I resolved all 10 children's live state (`gh issue view`) + re-ran the acceptance grep before closing, and matched the mechanic (unclaimed tracker → `gh issue close`, no `pmtools release`). |
| [TIL 2026-07-06 CHERRY (ruff)](./today-i-learned-2026-07-06-cherry-ruff.md) | 2026-07-06 | CHERRY | `$?` after a pipe is the *pipe's* exit, not the check's — `ruff format --check ... \| tail -2; echo $?` printed 0 while ruff was exiting 1, nearly reporting a red format hook green; only the visible `Would reformat` line contradicted it, so never judge a check by `$?` after a pipe (run un-piped / `${PIPESTATUS[0]}`, err #89). Widening a guard surfaces pre-existing rot — #540's hook widening exposed format drift in two untouched `pycats/` files (`git diff origin/main -- pycats/` empty), so I shipped #540's deliverable + filed a *scoped* #642 for exactly those files rather than blanket `ruff format pycats/` (the #597/#76 over-scope trap) or leaving main silently red. "Verify before asserting" covers a living-doc flip too — #625 confirmed both clones existed on disk *and* matched their upstream origins before flipping ⏳→✅, because a wrong ✅ is worse than the ⏳ it replaces. An acceptance criterion must be feasible against the tool's real output — I authored "`git diff -w` empty" into #642, but `ruff format`'s signature wrap adds a *magic trailing comma* (a real token), so it can never pass for that file; behavior-preservation proof is the green suite + inert-token reasoning, not `diff -w` (err #92). |
| [TIL 2026-07-06 FIG](./today-i-learned-2026-07-06-fig.md) | 2026-07-06 | FIG | Grill a design by grounding each claim in the code it assumes, not by re-reasoning — the `/grill-with-docs` pass on #611→#620 reversed two load-bearing spec claims (the "verbatim quote" rule is impossible for `TUNED`/`DIVERGENCE` values with *no source by construction*; the #575 consistency test couldn't have caught the ledge bug, which lived in prose not in the compared fields) and on #643 killed a "divergences view" the registry already *is* as a one-line filter. Apply a discipline to its own roadmap — murphy-jutsu's complexity-invoice flag cut the protocol MVP from 5 pieces to the 2 that prevent *observed* failures (#624), deferring freshness/tally/lint as guessed-need. Ground, don't invent — the ratified read-the-source rule (#562) fired minutes later on #643: instead of picking a plausible tracking issue for `DODGE_FRAMES`/`DODGE_TIME` I searched and read #65's body to confirm scope before attributing. Editing `combat/provenance.py` cascades — a local-looking registry edit made the *generated* `docs/parity-status.md` stale (errs #95/#96), regen via `python parity_report.py`, caught only by the full suite. The pycats gate is `ruff check`, not `ruff format` — #496's whole-tree "186 files would reformat" is the tell the formatter isn't enforced; use `ruff check --fix` for F401/I001 fallout, never `ruff format` across untouched files (matches #76/#82 over-scope traps). |
| [TIL 2026-07-06 BANANA (session 2)](./today-i-learned-2026-07-06-banana-2.md) | 2026-07-06 | BANANA | Measure decision churn by tickets-to-settle, not git value-diffs — #632's naive diff-count ranked `DODGE_SPEED`/`GROUND_FRICTION` top, but the costly `SMASH_CHARGE_*` changed `config.py` once yet dragged a 5-ticket arc (#581 booked Brawl values `FOUND`, #599 reversed). A naive `git -G"^NAME ="` count over-reported 27 churned constants vs 8 real, because two whole-file commits (ruff reformat #505, Axis-A #408) touch every assignment line — compress on the *value*, a line-touch isn't a value change. Verify the data substrate before scoping a sweep — #644's backfill was scoped off native GitHub sub-issue links, but 0 of 98 open issues had a usable one (trees are textual), so I surfaced the mismatch and switched to a mapping-approved semantic sweep instead of shipping an empty result. Same-size config edits defeat `.pyc` invalidation in revert-checks — `59`↔`60` within one second served stale bytecode → 6 false suite failures (#627, err #86); monkeypatch or clear `__pycache__`. Survey the tools the repo already owns before designing new machinery — #653's escalation rubric *composes* existing ICE (priority) + RULES "Changing values" (evidence) + personas (authority) and adds only the one missing axis, reversibility — the axis churn actually turns on. |
| [TIL 2026-07-06 ELDERBERRY](./today-i-learned-2026-07-06-elderberry.md) | 2026-07-06 | ELDERBERRY | Reproduce before fixing — #547's premise ("no ASCII fallback map") was already false for every live glyph (the map dates to the 2025 unicode commit); the one real red was `▶` (U+25B6, unused), and I kept `✓→"OK"` over the ticket table's `"x"` because a checkmark rendered `"x"` inverts to a reject. Search for the umbrella that already exists — the requested keyboard-controls epic *was* #476; I reorganized its body around the three questions and folded orphan siblings #554/#555 into its checklist instead of minting a duplicate. A red masked by an import error isn't an able-to-fail proof — #650's six tests errored at collection, so I physically reverted the wiring line (`palette_for(palette or char)`→`palette_for(char)`) and watched calico≠tabby go red before trusting it. A placeholder needs a discriminator no real instance carries — mid-gray `128` collided with the `tabby` skin, so testcat became fully achromatic *including the eyes* (no cat has gray eyes); and tracing "can I see it?" through both selection gates + `build_players` (which hardcodes calico/tabby) showed exposing it (#648) needs palette wiring, not a one-line `choices` add. A broken check command fails silently-wrong — #628's `find … -o -print \| grep` size-check exited 1 with empty stdout (false FAIL; err #85), caught via the evidence log not the scorecard, and the audit config had to match pycats' real shape (no pyproject/CI). The capability may exist un-enabled (pmtools#106's pre-close verify gate isn't set in orchestrate.json → #633) and `gh` validates labels before creating (a nonexistent `sequenced` label aborts the create atomically — safe to retry after verifying no partial issue). |
| [TIL 2026-07-06 DRAGONFRUIT (session 2)](./today-i-learned-2026-07-06-dragonfruit-2.md) | 2026-07-06 | DRAGONFRUIT | A "create `<path>`" ticket goes stale when the deliverable already exists — #535 asked to *create* `project-m-rules-by-category.md`, but `ls` + `git log` showed it built by #538/#543/#635 and test-gated, with the register materialized as `docs/pm-reference/` (19 files); grep the target path before claiming a create-ticket. Know what a local clone *is* before trusting a value — of our three, `meleelight` is a **Melee** reimpl (`chargeFrames === 60`, `invincibleTimer = 120`, all Melee-tier), `brawllib_rs` is a **parser** with no game data fed in, and `pm-data` is an **empty dir**; a local Melee source still yields a Melee-tier value, never a PM confirmation (PM's 59-frame charge and 1.3671 stay unverifiable locally). When a ticket's acceptance is only partly met (the register table was ledge-only, 1/3), re-scope to the unmet delta — closing-as-done lies, building-fresh duplicates — so I rewrote #535 "create"→"backfill invuln+respawn rows (~90m→~20m)" + pinned a why-comment, left open/unclaimed. Two `/eli5` asks in a row means the *framing* not the *content* is the blocker — cut jargon and the reflexive "you decide" hedge and name the plain shape ("the doc exists but is 1/3 filled → finish it"), don't re-argue (mirrors BANANA #584 give-the-opinion). |
| [TIL 2026-07-06 APPLE](./today-i-learned-2026-07-06-apple.md) | 2026-07-06 | APPLE | "File the ticket" ≠ claim+commit — asked only to file, I also claimed #652 and committed a seed doc (err #100); the session's scope errors cut both ways (over-acting #100/#57/#58, under-acting #88), so match the exact verbs, and "we will commit" (future) isn't "commit now." A Gecko codeset (`RSBE01.gct`) is a diff over `main.dol`, not a snapshot — #649's byte-scan for `1.3671`/`0.3671` found nothing because it holds only PM's *changes*, has no symbols, and omits unchanged values; verify an engine global by resolving a code's target *address* against `doldecomp/brawl`'s address-indexed `symbols.txt` (34,802 syms), never by grepping the value (#652 §6). Pin provenance with a *matched published checksum* + vanilla-vs-fork — the #640 codeset MD5 matched the mirror's published hash, and pmunofficial's "3.6+mf" is a fork, not the vanilla `pm36-canonical-reference` baseline. Retract your own optimism in the artifact — I'd called the GCT "a clean offline primary"; #649/#652 disproved it, so I wrote an explicit "Correction — retracts the earlier optimism" line rather than course-correcting silently. A doc is saved only once it's on `main` via `Closes #N` — a commit on an open ticket's branch (`refs #652`) is provisional, lost on release/re-claim, so release→docs-ticket (#655)→rewrite. Report success from the observed end-state, not a command's exit — `install_dolphin` logged "installed" after a failed sudo (err #91, fixed to check `flatpak list`), and `pmtools close` exits 1 from the worktree while succeeding (`CLOSE OK`, err #106). |
| [TIL 2026-07-06 BANANA (session 3)](./today-i-learned-2026-07-06-banana-3.md) | 2026-07-06 | BANANA | No quotable primary → cite the nearest quotable anchor, name the unreachable source of truth, and label the derived value ⚠ best-guess, never laundering a guess into `FOUND` (#659 PM FD floats live in-`.pac`; used Melee/libmelee as the anchor). When faithfulness and a hard engine constraint genuinely conflict — FD's blast zones land ~848px off a fixed 960×540 camera — surface the tradeoff as a `decision:` and ship the buildable remainder (the flat ground), don't guess a scale (#659→#660). Route through the seam the code already has: `platforms` was a caller-provided arg, so switching only `game.py`'s list gave players-FD / sims-Battlefield with zero golden churn (#660). When N subagents feed a committed artifact, extract their JSON programmatically from the `.output` transcripts — hand-transcription mis-typed #493's area call, caught only by the extractor (#679). ICE ranks by quick-win, so `decision:` tickets (E=10 to "author a ruling") float above real build work — read it through bug→blocker→ICE + human-routing, don't present a raw board (#679). Verify a phrase landed in wrapped prose line-agnostically (`tr '\n' ' '` before grep) — a word-wrap gave a false-negative on a correct CLAUDE.md edit (#600). |
| [TIL 2026-07-06 CHERRY (ledge)](./today-i-learned-2026-07-06-cherry-ledge.md) | 2026-07-06 | CHERRY | A plain-English ELI5 is still bound by verify-before-asserting — re-explaining my #531 work I asserted "the HANG bar owns that clock" during ledge-hang, but #475 had deleted that bar (`timer_bar_specs(_fake(state="ledge_hang")) == []`); a re-render inherits the claim's truth-obligation (errs #102/#105). A feature can pass its test *and* acceptance yet never run — #531's INVULN bar suppressed on `state != "ledge_hang"`, but `ledge_invuln_timer > 0` holds *only while* hanging, so the gate and the live condition are mutually exclusive; it "worked" only in a `_fake(timer=N, state="idle")` fixture that can't occur (err #103) — prove the gate reachable against real transitions, not a fake. Grep the open queue before filing research — the "validate 5-grab" ask was already #536+#552. Never predict a golden away — the #552 spike's Q5 "no regen" was wrong (a fighter *passively* grabs a ledge; `full_match`/`two_npc` diverged at a `ledge_hang` frame 391/126), safe only because the semantic `.summary.json` sidecars were unchanged; run the suite + review the sidecar per REGEN_PROTOCOL, never rubber-stamp (err #124). After deleting a constant, grep repo-wide — the #552 plan listed only `test_edge_hog.py` but `test_ledge_hang.py` also used the retired `LEDGE_INVULN_MAX_FRAMES`. rukaidata shows subactions not engine globals — PM 3.6 `CliffCatch`=21f was datamined, but the post-cutoff residual is engine-hardcoded (the `DODGE_AIR_SPEED` limit, #215/#222). |
| [TIL 2026-07-06 DRAGONFRUIT (session 3)](./today-i-learned-2026-07-06-dragonfruit-3.md) | 2026-07-06 | DRAGONFRUIT | Before a new code path, check the seam is already there — #662's live skin preview was just passing the `palette_key` override #650 already added into a per-player-offset preview cat, resolving the same-character overlap #650 deferred, no new render path. A discoverability audit is per-screen verdicts with `file :: symbol` citations *and named alternatives* (hold-B discoverable via `screen_render.py::render_active_screen`; hold-ESC not, but reachable via Quit button / pause menu → hint-gap not bug), follow-ups proposed **not filed** per out-of-scope (#549). A self-contradicting instruction is a fork, not a typo — the human's "by the above logic" inverted their own point-1 toggle split for the ESC-hold, so I put both mappings up via `AskUserQuestion` rather than silently fix or blindly encode; they corrected (#681). The env-at-import footgun recurred *despite* an existing memory note — `os.environ[...]=` at test-module scope leaked into the session and swung green→17-red across untouched files (err #127); a known-footgun note doesn't fire where it's invisible, so guard mechanically with per-test `monkeypatch.setenv`. For a gating feature, test the *mapping's distinctness* not each output's presence — the able-to-fail assertion proves the battle ESC-hold hint stays under the OG toggle when the NEW one is off (catches a swapped wire; presence alone wouldn't), and a byte-parity oracle (`test_shell_chrome`) must grow the same line the function did or it mirrors the wrong thing. |
| [TIL 2026-07-06 ELDERBERRY (session 2)](./today-i-learned-2026-07-06-elderberry-2.md) | 2026-07-06 | ELDERBERRY | To clone two files out of an 824 MB build, blobless partial + cone sparse-checkout pulls ~8 MB (`--filter=blob:none --no-checkout` → `sparse-checkout set` the dirs), and bracket-named dirs need `--skip-checks` (`[]` are glob-special); the acceptance was a *cross-check* — `wc -l == 8044` proves the text file is the same codeset as vanilla `RSBE01.gct`, not just that a file arrived (#664, err #111). In a provenance register, `Status` is the *canon finding's* provenance (`FOUND` = sourced) while the value column is *implementation state*, so a `FOUND` row legitimately reads **unimplemented (#506)**; the keyed row must lead with the registry's token (`RESPAWN_DELAY_FRAMES`→`TUNED`) or the #635 manifest↔registry gate reds (#535). Ask filing-depth first (minimal/moderate/thorough), then let the spike earn its length by *drawing scope fences* — #682's value was distinguishing a new per-player slot row from the per-character grid, the #662 preview, and #676's tile-recolor so it didn't duplicate any. An additive render change regenerates nothing *when you know why* — `screen_parity` is an FSM trace and no byte-golden covers char-select, so a purely-additive row flips nothing (confirmed by running the suite, not assuming), yet still revert-check the additive feature (drop `palette_key` → P1/P2 slots red). A consumer can outrun its migration plan — #682 landed a new `char_select` cosmetic call site *after* #680's Phase-1 plan and #676 were written, so no phase ticket named it; the right move was a seam-note on epic #672 + a `blocked` re-audit ticket (#689), not a premature fix, and re-deriving live ownership from the worktree branch (`wt-grape-…-686`) caught that "Phase 1b not yet filed" had gone stale mid-session. |
| [TIL 2026-07-07 GRAPE](./today-i-learned-2026-07-07-grape.md) | 2026-07-07 | GRAPE | Append to a serialized golden tuple, never insert mid-way — #695's `PlayerSnap.character` after `name` (per the DP2 preview) shifted every index and silently broke production `sim/battle_log.py`'s positional reads (`_STATE=1,_PERCENT=7,_LIVES=9`) + ~9 test files (22 failures); appending last preserves every index (err #130) → grep positional readers before repositioning a serialized field. Prove byte-identity mechanically, not by eye — `git diff tests/golden/` empty for the golden-neutral phases (#680/#686/#692), and a strip-the-appended-field comparison across all frames/scenarios confirmed #695 added *only* the field. A golden *flip* is gated by the review not the green suite — post-regen green is circular, so I committed then held the close, posted the `.summary.json` digest, and got the game-designer's sign-off before `pmtools close` (#695, author≠reviewer). Render-cache proxies duck-type your entities — #692's `slot_accent_color`/`draw_player_name` also run on a `_CatShim` (a slots-based body-composite cache proxy with no `identity` seam), so "byte-identical, no test change" was wrong; I deferred the render-accent repoint to a render slice (#672 "1d") + grew 2 win-screen doubles (err #128). Sequenced the #672 decomplect epic as small golden-posture-labeled children: 1a/1b/1c golden-neutral → 2a schema-add → 2b mechanics-flip. |
| [TIL 2026-07-07 FIG](./today-i-learned-2026-07-07-fig.md) | 2026-07-07 | FIG | When two things are coupled by a *policy* not a *need* (save-after-toggle), compose them at the layer that owns the policy — don't inject the second into the first; the grill reframed my `persist`-callback for `DisplayManager` (#280→#698) into a pure object that takes plain values, one reason to change, no file-I/O in tests (#345 trap dodged). A closed dependency can *defer* the question, not answer it — #280 said "defer to #257", #257 closed but its comments show it flagged-and-scoped-out the shell question ("no action needed mid-spike"), so I read the outcome + re-verified `game.py`'s premises before calling #280 ready (an instance of #562 read-before-asserting). When the safety net is an FSM-*trace* (`screen_parity`) not pixels, a green suite is not a pixel guarantee — prove a render refactor byte-identical with a before/after `pygame.image.tobytes`→sha256 across the state matrix (#515 arc, #508, baked into #698's acceptance; `render-hash-verify-uncovered`). Grill-then-guide turns a recommendation into a *recorded* ruling — `grill-me` down the tree one node at a time is where the load-bearing sub-decisions (compose-not-inject) actually surface, then `guide-human-decision` posts the ruling + files only the next slice per one-at-a-time cadence (#280 closed → #698 filed). Shipped the demo/sim playback trio #514/#515/#508. |
| [TIL 2026-07-08 GRAPE](./today-i-learned-2026-07-08-grape.md) | 2026-07-08 | GRAPE | A monkeypatched *factory* leaks its fake through a *shared cache* — `test_main_menu_title`'s `monkeypatch` of `pygame.font.SysFont` was correct, but `TextRenderer._get_font` cached the `_DummyFont` in the shared `text_renderer.font_cache`, so it outlived the revert and reddened `test_menu_button` on reorder; flush the downstream cache in teardown (RULES cached-fake variant, #550→#709). A red gate is not a PDD puzzle — #550's own tests made the suite red-on-reorder, and merge-gate/zero-tolerance outrank PDD, so you can't `@todo`-and-move-on; root-fix only (A trivial-inline+track, B non-trivial-stop-and-fix-first), the ratified refactor-exposed-failure protocol. A leak-detector that *evicts* beats one that only asserts — `_no_fake_fonts_leaked` clears the fake and fails the polluter by name (proven able-to-fail by disabling #550's flush: fired on `test_main_menu_title`, victims stayed green). `git checkout <file>` during a revert-check reverts to HEAD and eats an uncommitted addition (wiped `HUD_EMPHASIS_SIZE`, err #137) — edit in place instead. Shipped the HUD emphasis (#550), the guard + RULES protocol (#709), and the font-usage guide (#711). |
| [TIL 2026-07-08 BANANA](./today-i-learned-2026-07-08-banana.md) | 2026-07-08 | BANANA | An acceptance criterion is only real if its test can be *written at that slice's stage* — #701's "loop-wiring / QUIT-path" bullet couldn't be tested until C3's `step()` seam existed (the body was welded inside `while running:`), and the #280 ruling already placed it in C3, so trimming it was rung-1 authority, not opinion (trim not split: the wrap + its proving test are one complaint). When the pre-fix state *hangs* rather than fails, prove able-to-fail with a fast-side-effect revert-check — importing unwrapped `game.py` blocks forever, so I watched the AST guard red-then-green and injected a module-scope `pygame.init()` to redden the monitored-import assertion (`Left contains one more item`). Ground interlocking design forks in the codebase's own idioms, one at a time — `grill-with-docs`+`guide-human-decision` on #707 resolved poll-injection / settings-boundary / drive-location in dependency order, each pointing at a precedent (`ScreenStateManager(display_hooks=None)`; `DisplayManager`'s plain-values). `compose, not inject` propagates up the layers: `main()` loads prefs and hands `App` a plain dict, so the four `step()` wiring tests construct file-I/O-free (the #345 trap dodged) while `App` still *owns* `save_prefs`. Dead code a refactor *surfaces* (F841 on the wrapped-in dead `font` var) is forced work — do it, but flag it in the commit + closing comment, never silent. Completed the #280 C1→C2→C3 shell extraction (#698/#701/#707), closing #386; also DP1 placeholder render (#694). |
| [TIL 2026-07-09 DRAGONFRUIT](./today-i-learned-2026-07-09-dragonfruit.md) | 2026-07-09 | DRAGONFRUIT | The cheapest ticket is the one you don't file — two "add stock counts" requests were already shipped (`sim/presenters.py::_draw_overlay` draws "N stocks"; the live HUD draws an always-on "Lives: N"), so I grepped the render path and surfaced it instead of filing no-ops (RULES verify-before-filing). A new convention silently reclassifies the tickets it touches — building the append-only Decision Log (#705/ADR-0007), appending a ledger row makes a `decision:` ticket a *committed artifact*, so it must close via `Closes #N`+`pmtools close`, not the commit-less `gh close`+`pmtools release` path; the spec's own "alongside the no-code path" framing was self-contradictory and I fixed the RULES clause. Read the repo's storage conventions before proposing where something lives — `grill-with-docs` sent me to RULES+`provenance.py`+`docs/adr/`, which showed decisions already record as ratified-ticket + `TUNED` + ADR and that my suggested `docs/decisions/` didn't exist (#704 review). A closed ticket's wrong words outlive the work — #726 said "player color" but my own wording leaned skin/palette; I shipped the P1/P2 *slot* accents, stated the interpretation in the commit, then *redlined the closed ticket* (strikethrough + top correction) so future agents aren't re-confused. An advertised range can lie about the behavioral range — the CPU ladder "1–9" snapped to 5 anchors (ties round **up**: Lv2≡Lv3, Lv4≡Lv5…), visible only by reading `level_params` not the anchor table; fixed with half-up interpolation (#703). Shipped #703/#705/#710/#715/#726; #704 decision approved (near-miss + accidental-press + per-character CPU tuning as pycats-custom). |
| [TIL 2026-07-19 GRAPE](./today-i-learned-2026-07-19-grape.md) | 2026-07-19 | GRAPE | Claimed work lives in the worktree — edit, test, AND surface run/visual-review commands from the worktree dir, not the main checkout (which only sees a change after merge; err #150/#158, owner caught the run command pointing at main). `git checkout <file>` to undo a revert-check reverts to HEAD and eats the whole uncommitted implementation — restore the mutated hunk in place instead (err #157, recurrence of #137). `pmtools claim` minted branches missing the `issue-<N>` token, which `pmtools close`'s `find_worktree_for_issue` can't resolve (failed from main, from the worktree, and with `--branch`); `git branch -m` to `br-<fruit>/<proj>-<lang>-issue-<N>-<slug>` before closing (err #151, then renamed proactively). Test a scenario only after confirming the state machine reaches it — #676's "both confirmed then cycle" is unreachable (the start overlay disables cycling), so I asserted last-confirmer-owns-tile on `_active_skin_by_char` (the state the render reads) not an overlay-confounded pixel. A consumer of a ratified domain seam calls the layer, never re-implements it — #755's `available_skins`/`assign_distinct_skins` made #718 a 1-line de-collide and #676's pool+lock thin calls, fixing the char-select global-pool bug for free. Green ≠ looks right — held #676's close for the owner's eyeball (visual check-in gate) before `pmtools close`. Shipped #718 + #676, closing the #755→#718→#676 skin/Character chain. |
| [TIL 2026-07-19 APPLE](./today-i-learned-2026-07-19-apple.md) | 2026-07-19 | APPLE | In a mixed skill migration let **git-trackedness draw the commit boundary** — the git-excluded `claims-data/` reshape (new-model verify-claims: root+topics, drop `evidence/`+per-topic `INDEX.md`, add `draft`/`cancelled` files) needs no ticket, but the one tracked file (`.claude/ledger.json`, migrated to the 7-key schema) routes through its own chore (#757), never the research branch it was discovered on. A tool rejecting a **canonical** name is the consumer's bug: `pmtools close` couldn't resolve the #128 `standard` worktree shape because `find_worktree_for_issue` still hardcoded the dropped `issue-` token — two separate fixes (pycats `worktreeBranchPattern`/RULES drift #759; the incomplete #128 migration upstream **pmtools#135**, which GRAPE also hit as err #151). **Left red on main** because I ran the suite *before* my final `ledger.json` edit and the `pmtools close` gate is **ruff-only, never pytest** — a green close is not a green suite (#762 fix, #764 rule); grep the repo for a test guarding any config file you change. Dogfooded pmtools#135 by closing #762 with a standard-form worktree and no `git branch -m`. Demoted 3 self-verified `tooling/` claims (asserter=verifier=CLAUDE) verified→unverified — MOVE not copy, keep the ID; a self-cert is unproven, not `bad`/`FALSE`. |
| [TIL 2026-07-19 BANANA](./today-i-learned-2026-07-19-banana.md) | 2026-07-19 | BANANA | Run a ticket's machine-checkable acceptance *during review* — I executed #759's proposed `worktreeBranchPattern` regex against its own acceptance inputs before approving, turning "looks right" into "verified." Run the full suite the moment you claim (fleet merge race): it caught `test_ledger_config.py` red on `main` — a pre-existing defect from #757's incomplete migration, not #759 — and an ambiguous pre-existing red is surfaced as its own ticket (#762), never silently folded into yours. `git blame`/`git log` root-caused it: `1cc6ddb` (#757) dropped the defunct `evidenceDir` from the config but not from the guarding test, and its close comment self-certified "Suite green" — mechanically impossible, because the ruff-only `pmtools close` gate never runs pytest, so a red suite merged (the rule is now codified as #764). Never state a ticket's open/closed status from memory or another ticket's body — I called `pmtools#135` "still open" from #759's stale out-of-scope note; `gh issue view` showed it CLOSED. When a stated fact ("759 is closed") contradicts the tree, verify and report the discrepancy with evidence, don't build on it. Shipped #759 (worktree/branch-shape config+docs sync to the pmtools #128 standard). |
